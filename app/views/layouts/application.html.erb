<!DOCTYPE html>
<html>
  <head>
    <title>BarTrainer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <h1> Bar Trainer </h1>
      
      <% if current_user %>
      <ul>
      <li><%= current_user.email %></li>
      <%= link_to "Add a new Cocktail", new_cocktail_path %> |
      <%= link_to "View my Recipes", user_cocktails_path(current_user) %> |
      <%= link_to "View all Cocktail Recipes", cocktails_path %> |
      <%= link_to "View all Comments", comments_path %> |
      <%= link_to "Log out", logout_path, method: "delete" %>
      </ul>
      <% else %>
      <%= link_to "Login", login_path %></a> |
      <%= link_to "Signup", signup_path %></a> |
      <%= button_to "Log in with Google", '/auth/google_oauth2' %>
      <% end %>

    <div class="container"  role="main">
      <% if flash[:notice] %>
        <%= flash[:notice] %>
      <% end %>

      <%= @error %>
      <% if flash[:message] %>
        <%= flash[:message]%>
        <% end %>
      
      <%= yield %>
    </div>

    <footer class="footer fixed-bottom">
      <span class="text-muted small mb-0">© Copyright 2021 Bar Trainer</span>
</footer>
  </body>
</html>