<h1>Edit Comment!</h1>

<%= form_for(@comment) do |f|%>
  <%= f.label :cocktail %>
  <%= f.collection_select :cocktail_id, Cocktail.all, :id, :name %>
  <%= f.label :comment %>
  <%= f.text_field :comment_text %>
  <%= f.submit %>
<% end %>